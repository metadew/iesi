server:
  servlet:
    context-path: /api
  error:
    whitelabel:
      enabled: false
springdoc:
  version: '0.3.0'
spring:
  ldap:
    url: ldap://localhost:10389
    base: DC=example,DC=com
    username: CN=khatth,OU=test,DC=example,DC=com
    password: hErebih0Nlph99
    base-environment:
      com.sun.jndi.ldap.connect.timeout: 500
  profiles:
    active: http
  data:
    web:
      pageable:
        one-indexed-parameters: true #Allow the UI to query page 1 instead of 0
iesi:
  security:
    enabled: true
    client_id: iesi
    client_secret: tBKjN9Sy2TBgahGd
    jwt:
      secret: secret
      access-token-validity: 1800
      refresh-token-validity: 86400
  debug: true

management:
  endpoints:
    web:
      exposure:
        include: health, httptrace, metrics, mappings
  health:
    ldap:
      enabled: false
  trace:
    http:
      include: request-headers,response-headers,cookie-headers,principal,time-taken,authorization-header,remote-address,session-id