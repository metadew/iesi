encrypt:
  key-store:
    location: classpath:iesiCloud.jks
    password: iesipassword
    alias: iesi
    secret: password

spring:
  profiles:
    active:
    - native
  cloud:
    config:
      server:
        native:
          search-locations:
          - classpath:config/