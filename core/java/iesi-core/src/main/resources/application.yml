spring:
  main:
    banner-mode: "off"
iesi:
  guard:
    authenticate: N
#  script:
#    execution:
#       runtime: ''
  server:
#    mode: off
    mode: standalone
    threads:
      timeout: 30
  home: '.'
  security:
    encryption:
      key: c7c1e47391154a6a
    enabled: true
    jwt:
      secret: secret
      expiry-date: 10
  workers:
    - path: '.'
      timeout: 30
  master:
    path: '.'
    host: 'http://localhost:8080'
    credentials:
      user: admin
      password: admin