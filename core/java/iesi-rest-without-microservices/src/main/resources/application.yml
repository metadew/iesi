server:
  servlet:
    context-path: /api
  error:
    whitelabel:
      enabled: false
springdoc:
  version: '0.3.0'
spring:
  ldap:
    url: ldaps://adlds.belwired.net:636
    base: DC=Belwired,DC=Net
    username: CN=khatth,OU=Business,OU=Users,OU=Default,OU=IAM-10-Default,DC=belwired,DC=net
    password: W9T2s!fe
    base-environment:
      com.sun.jndi.ldap.connect.timeout: 500
  profiles:
    active: http
  data:
    web:
      pageable:
        one-indexed-parameters: true #Allow the UI to query page 1 instead of 0

iesi:
  security:
    enabled: true
    jwt:
      secret: secret
      expiry-date: 3600
  debug: true

management:
  endpoints:
    web:
      exposure:
        include: health, httptrace, metrics, mappings
  trace:
    http:
      include: request-headers,response-headers,cookie-headers,principal,time-taken,authorization-header,remote-address,session-id