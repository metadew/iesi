package io.metadew.iesi.server.rest.dataset;

public class DatasetFilter extends Filter {

    public DatasetFilter(DatasetFilterOption filterOption, String value, boolean exactMatch) {
        super(filterOption, value, exactMatch);
    }
}
